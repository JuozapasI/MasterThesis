\section{Data Acquisition}

\paragraph{\textbf{scRNAseq datasets}}

We analyzed datasets from four different human tissues: brain, blood, lung, and eye.
All samples, except for two Peripheral Blood Mononuclear Cell (PBMC) datasets,
were generated using the 10x Genomics v3.1 protocol (\cite{Juzenas2025}).
The two exceptions were prepared using the inDrops2 protocol.
All protocols capture 3' ends of RNA molecules and use short-read sequencing.
The datasets are publicly available, with sources and additional details provided in Table \ref{tab:datasets}.

\input{"textparts/tables/datasets.tex"}

\paragraph{\textbf{Genome and Transcriptomic references}}

The human genome assembly GRCh38 was used in this project, downloaded from the \href{https://ftp.ensembl.org/pub/release-113/fasta/homo_sapiens/dna/}{Ensembl website}.

Four transcriptomic references were analyzed (see Table \ref{tab:references} for details).
To ensure compatibility with the genome FASTA file, chromosome name prefixes ('chr') were removed from references that included them,
as the genome file does not use these prefixes.
This modification was performed using basic Linux command-line tools.
Additionally, for the NCBI reference, chromosome names were converted to Ensembl-style notation using a custom Python script
(e.g., 1, 2, 3 instead of NC\_000001.11, NC\_000002.12, NC\_000003.12, etc.).

For references that do not have 'gene' entries, such entries were added
(entries that span all the components of a particular gene).

\input{"textparts/tables/references.tex"}

\paragraph{\textbf{Gene Prediction Tracks and Conservation Scores}}

Gene predictions (tracks 'AUGUSTUS', 'Geneid genes', 'Gescan genes', 'SGP genes', 'SIB genes') and 
genome conservation scores ('phastCons100way' track) were downloaded from \href{https://genome.ucsc.edu/}{UCSC genome browser}.

\paragraph{ATAC data}

The open chromatin regions (bed format) in PBMCs was downloaded from \href{https://www.10xgenomics.com/datasets/10k-human-pbmcs-atac-v2-chromium-controller-2-standard}{10x website}.

\paragraph{Oxford Nanopore Technologies (ONT) data}

PBMC ONT data (full-length cDNA) were downloaded from \href{https://www.10xgenomics.com/datasets/5k-human-pbmcs-3-v3-1-chromium-controller-3-1-standard}{10x genomics website.}

\paragraph{Viral genomes}

Viral genomes for blasting unassigned sequences for BLAST searches were downloaded
from \href{https://ftp.ncbi.nlm.nih.gov/genomes/refseq/viral/}{NCBI Refseq genomes database}.

\section{Computational Tools and Environment}

All analyses were performed on a high-performance computing (HPC) cluster running a Linux environment.
Software packages and tools were managed using Conda.
The exact Conda environment specifications (YAML file) can be found on \href{https://github.com/JuozapasI/MasterThesis/}{GitHub repository}.
Besides tools managed by conda, also basic command line tools were used (e.g. \textit{awk, wc, grep, sed, uniq, sort} and similar).
The specific functions and parameters used are described in the following sections, where the general analysis pipeline will be explained.

\section{Data processing pipeline}

The pipeline was implemented using GNU MAKE and the full scripts are available on the \href{https://github.com/JuozapasI/MasterThesis/}{GitHub},
here only general description of the workflow and used tools is provided.
The general scheme of the pipeline (used both for extracting intergenic regions and enhancing transcriptomic reference)
can be seen in Figure \ref{fig:pipeline}, while the description is given below:

\begin{enumerate}
    \item Map reads with initial transcriptomic reference.
    \item Take unassigned (and uniquely mapped) reads.
    \item Split into intersecting and intergenic reads.
    \begin{enumerate}
        \item For intersecting:
        \begin{enumerate}
            \item Resolve overlapping genes that have unassigned reads (if possible).
            \item From the second reference and further: add genes to the original GTF that contain unassigned reads
            and do not overlap with entries from the original one.
        \end{enumerate}
        \item For intergenic:
        \begin{enumerate}
            \item Cluster.
            \item Filter-out relatively small clusters (custom threshold).
            \item For the first reference only: filter-out AT-rich reads.
            \item For reads that are left, repeat from the beginning with the next reference.
            \item For the last reference only: clusters that start just after 3' ends are assigned to genes (i.e., extend genes).
        \end{enumerate}
    \end{enumerate}
    \item Create final GTF and map initial sequences to it.
    \item Create list of large (\textgreater 5CPM) intergenic clusters.
\end{enumerate}

\begin{figure}
  \centering
  \includegraphics[width=\linewidth]{images/pipeline.png}
  \caption{Scheme of the pipeline. This sheme provides only general logic and main tools of data processing.
  The pipeline itself is more complex, e.g., using multiple transcriptomic references required looping over some of those steps.}
  \label{fig:pipeline}
\end{figure}

\paragraph{Mapping and filtering bam files}
Reads were mapped using STARsolo (\cite{Kaminow2021}), both from FASTQ and BAM input files.
The same parameters were used for all samples (see \href{https://github.com/JuozapasI/MasterThesis/}{GitHub repository}),
except the ones regarding barcode structures
(differences include number of barcodes, lengths of barcode/UMI sequences, and their positions in the primer).
Filtering unassigned reads was done using \textit{awk}, taking those that have valid barcode
(i.e. filtering out those reads that have barcode length not equal to the defined length)
and were not assigned to any gene (i.e. had 'GN:Z:-' tag).
Also, only uniquely mapped reads were taken (tag 'NH:i:1').

\paragraph{Classifying reads as 'intergenic' or 'intersecting' and reads clustering}
Intersections of unassigned reads with references were checked using \textit{bedtools intersect} command.
Those that intersect with genes were classified as 'intersecting', and those that do not – as 'intergenic'.
Intersections were assesed in strand-specific manner (bedtools '-s' flag), i.e. regions on different strands were treated as not intersecting,
even if they share overlapping genomic coordinates (see Figure \ref{fig:intergenicIntersecting}).
The intergenic reads were clustered using \textit{bedtools merge}, again in a strand-specific manner.

\begin{figure}
  \centering
  \includegraphics[width=\linewidth]{"images/intergenic_intersecting.png"}
  \caption{Unassigned read classification as intergenic or intersecting.
  Reads are defined as intersecting if they overlap with known genes and are on the same strand.
  Remaining reads are defined as intergenic.}
  \label{fig:intergenicIntersecting}
\end{figure}

\paragraph{Manipulating transcriptomic references}

The overlapping gene resolving and construction of enhanced transcriptomic reference was done using custom R script,
particularly \textit{rtracklayer} library.

\section{Intergenic regions}

\paragraph{Extracting intergenic regions}

The intergenic reads were extracted as described in the previous section (i.e. taken unassigned reads that do not overlap with any reference used)
and merged into clusters using \textit{bedtools merge} command.
Clusters were filtered based on cluster size, requiring at least 5 reads per million (CPM),
where CPM represents the normalized read count per million of total primary reads in the dataset
(i.e. $1 CPM = 1 \times sample\_size / 1000000$).
The filtering was done using \textit{awk}.

Cluster locations were adjusted using deeptools (to compute coverage) and custom python script,
to avoid reads containing long introns making the intergenic clusters very wide.
To accomplish this, for each intergenic region, the maximum coverage location was found and extended to include neighbouring regions that had
at least half the maximum coverage.
In such way, intergenic regions were adjusted to cover only 'peaks' of reads.

The lists acquired from all samples were then merged using \textit{bedtools merge} function.
This combined list then was filtered to contain only regions detected at least
in all samples of one group ('eye', 'lung', 'brain', 'PBMC\_10x' or 'PBMC\_indrops').
Such filtering still allows to include tissue-specific regions in the combined list,
while removing those that are not persistent across the same type of samples.
Additionally, for each entry it was determined whether it overlaps with predicted genes from UCSC gene prediction archive
(using \textit{bedtools merge}) and distances to the closest genes were found (\textit{bedtools closest}).
Intergenic regions that were located on the opposite strand of known genes, were classified as \textit{antisense},
while those that were not were classified as \textit{isolated} (see Figure \ref{fig:antisenseIsolated} for illustration).

\begin{figure}
  \centering
  \includegraphics[width=\linewidth]{"images/antisenseIsolated.png"}
  \caption{Classification of intergenic regions as antisense or isolated.}
  \label{fig:antisenseIsolated}
\end{figure}

This filtered combined list was then converted into GTF format and unassigned reads from each sample were mapped using this
combined intergenic annotation.

\paragraph{Analysis of cell-gene matrices}

The matrices produced by STAR were filtered, allowing cells that have sufficient number of reads (thresholds selected manually),
and only genes that were expressed in at least 3 cells.
Also cells were filtered based on mitochondrial gene count (allowing up to 10\% of mitochondrial gene counts in a cell).
Doublets were filtered using \textit{scrublet}.
Afterwards, matrices were normalized (using \textit{normalize\_total} function from \textit{scanpy} package) and log-transformed
(i.e. for each entry $x = log(x+1)$, \textit{log1p} function from \textit{scanpy}).

Then only highly-variable genes (min\_mean=0.0125, max\_mean=3, min\_disp=0.5) were selected.
For the clustering, principal components (PCs) were computed and optimal number of them were selected based on elbow rule (manually).
For blood samples, cells were annotated automatically using CellTypist
(for other samples, annotation was skipped, as it was not in the main focus of this project).
Then visualizations were made using UMAP embeddings (functions from the same \textit{scanpy} package).

All the scripts with descriptions can be found in the \href{https://github.com/JuozapasI/MasterThesis/}{GitHub repository}.

\paragraph{Correlations}

Spearman's rank correlation between intergenic region and gene on the opposite strand was checked using custom python script.
Formula for Spearman's correlation is given by:

$$\rho = 1 - \frac{6 \sum d_i^2}{n(n^2 - 1)}$$

where $d_i$ is the difference between the ranks of corresponding values and $n$ is the number of observations.

\paragraph{Oxford Nanopores data}

Nanopores data were mapped using \textit{minimap2} algorithm and possible transcript structures were inferred using \textit{stringtie} tool.

\section{Enhancing transcriptomic reference}
\label{sec:overlappers}

Three aspects were considered while enhancing transcriptomic reference:
\begin{itemize}
  \item \textbf{Gene overlaps} – some of the genes are overlapping, resulting in unassigned reads from those overlapping regions.
  \item \textbf{3' end extensions} – for some genes, there are clusters of reads just after their 3' ends,
  and \textcite{Pool2023} suggested to include those reads by extending 3' ends for some genes.
  \item \textbf{Different annotations} – while most of the annotations agree on most genes,
  there is a small fraction of genes that are present in some references but not in others.
  Hence, for the enhancing transcriptomic reference, several transcriptomic references were used.
\end{itemize}

\paragraph{Overlaps}

The criteria for the resolving of overlapping genes are following:
\begin{enumerate}
  \item \textbf{Gene type}: prefer protein coding genes over other types, lncRNA over remaining (e.g. pseudogenes).
  \item \textbf{Level}: some annotations have 'level' field, indicating if the gene is verified (score 1), manually annotated (score 2)
  or automatically annotated (score 3). Lower 'level' score was preferred.
  \item \textbf{Intersection types}: if 5' end gene is overlapping with 3' end of gene, 5' end of gene was shortened.
\end{enumerate}

Particularly, gene types were divided into three groups: protein coding, long non-coding RNAs and other,
and each class was assigned score (see Table \ref{tab:geneTypes}).
The overall priority score was defined as sum of this gene type score and 'level' field.
The lower this score is, the higher priority gene has.
The intersection type (whether 3' end is overlapping or not) was taken into account only in cases when priority scores were equal,
and distances between 3' ends of overlapping genes were greater than 1kb (see Figure \ref{fig:overlappers} for graphical explanation).
If possible, lower priority genes were shortened, otherwise they were deleted.

\begin{table}[h]
    \centering
    \begin{tabular}{lcl}
        \toprule
        Class & Score & Gene Types \\
        \midrule
        protein coding & 0 & protein\_coding, IG\_C\_gene, IG\_D\_gene, etc. \\
	long non-coding RNA & 10 & lncRNA, lincRNA \\
	other & 20 & snRNA, snoRNA, pseudogene, etc. \\
        \bottomrule
    \end{tabular}
    \caption{Priority scores assigned based on gene type.}
    \label{tab:geneTypes}
\end{table}

\begin{figure}
  \centering
  \includegraphics[width=\linewidth]{"images/overlappers.png"}
  \caption{Strategy for resolving overlaps. \textit{p1} and \textit{p2} are priority scores of \textit{gene1} and \textit{gene2}
  (lower score means higher priority), while \textit{d} is distance between 3' ends of those genes.}
  \label{fig:overlappers}
\end{figure}

\paragraph{3' end extensions}

Unassigned reads clusters (of size at least 1CPM) that were close to the 3' of genes (closer than 100bp) were assumed to originate from those genes.
Hence annotations for those genes were expanded to cover those clusters.

\paragraph{Multiple annotations}

The initial transcriptomic reference (taken from 10x Genomics website) was enriched with entries from other annotations.
Particularly entries from GENCODE, RefSeq and lncpedia transcriptomic references were added to the initial reference,
if they overlapped with unassigned reads and did not overlap with genes already present in the reference.
This enrichment was done sequentially, i.e. firstly the reference was updated with entries from GENCODE annotation,
then resulting reference was updated with entries from RefSeq annotation and finally from lncpedia annotation.

\section{Unmapped reads}

Additionally, unmapped reads were extracted using samtools and mappings were searched against viral genome sequences (BLAST tool).
The tool was run locally, with the viral genome sequences for database construction acquired from \href{https://ftp.ncbi.nlm.nih.gov/genomes/refseq/viral/}{NCBI Refseq genomes database}.
